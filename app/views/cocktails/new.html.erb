<h1>Ajoutez un cocktail à la collection !</h1>

<%= simple_form_for @cocktail do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
  <%= f.submit "Ajoutez un cocktail", class: "btn btn-primary" %>
<% end %>
